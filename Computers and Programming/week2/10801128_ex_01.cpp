#include <stdio.h>
#include <stdlib.h>
int main()
{
    int day = 4;
    if (day==6) printf("Today is Saturday");
    else if (day==7) printf("Today is Sunday");
    else printf("Looking forward to the Weekend");
    system("pause");
    return 0;
}